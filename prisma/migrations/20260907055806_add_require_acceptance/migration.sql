BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[TypeOfFinding] ADD [requireAcceptance] BIT NOT NULL CONSTRAINT [TypeOfFinding_requireAcceptance_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
